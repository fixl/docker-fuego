services:
  trivy:
    image: aquasec/trivy:latest
    entrypoint: ""
    working_dir: /src
    volumes:
      - cache:/root/.cache/
      - .:/src
  anybadge:
    image: ghcr.io/fixl/docker-anybadge:latest
    entrypoint: ""
    working_dir: /src
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/src
volumes:
  cache:
